# RICE Docs Agent Guide

This directory owns image doctrine, prompt documentation, screenshots, and contact-sheet documentation.

- Read `../AGENTS.md`, `../ONTOLOGY.md`, and `MEMORY.md` before edits.
- Preserve archive ethics: generated archival images must remain labeled as visual reconstructions.
- Keep prompt docs and machine-readable prompt records aligned when either changes.
- Do not place secrets or private source material in docs.

Closeout:

- Run `git diff --check` for doc-only changes.
- Append a short entry to this directory's `MEMORY.md` and to `../MEMORY.md`.
